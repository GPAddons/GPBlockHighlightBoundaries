package com.github.gpaddons.blockhighlightboundaries.style;

/** Enum representing styles of highlights. */
public enum HighlightStyle {

  /** A highlight style where all elements are on a level plane. */
  FLAT,
  /** A highlight style where elements snap to nearby surfaces. */
  SNAP_TO_SURFACE

}
